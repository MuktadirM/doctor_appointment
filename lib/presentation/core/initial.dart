import 'package:flutter/material.dart';

class InitialData extends StatelessWidget {
  const InitialData({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Please wait....'));
  }
}
